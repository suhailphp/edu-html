/*
 * AuthForgotPage Messages
 *
 * This contains all the text for the AuthForgotPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthForgotPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthForgotPage container!',
  },
});
