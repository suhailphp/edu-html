// import { selectAuthSignupPageDomain } from '../selectors';

describe('selectAuthSignupPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
