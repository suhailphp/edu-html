/*
 * AuthSignupPage Messages
 *
 * This contains all the text for the AuthSignupPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthSignupPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthSignupPage container!',
  },
});
